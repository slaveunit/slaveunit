<div class="visible-lg-block visible-md-block">
<tr>
	<td><%= product.name %></td>
	<td><%= product.description %></td>
	<td><%= product.colour %></td>

	<!-- PRICE -->
	<td>US$<%= product.price %></td>
	<!-- PRICE -->

	<td><%= image_tag (product.photo), :style => "margin: auto", :class => "img-responsive" %></td>
	
	<td><%= link_to 'Show', product %></td>
	<td><%= link_to 'Edit', edit_product_path(product) %></td>
	<td><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></td>
</tr>
</div>



<div class="product-index-sm visible-sm-block visible-xs-block">

	<h1><%= product.name %></h1>
	<p><%= product.description %></p>
	<h3>Colour:<%= product.colour %></h3>

	<!-- PRICE -->
	<p>US$<%= product.price %></p>
	<!-- PRICE -->

	<p><%= image_tag (product.photo), :style => "margin: auto", :class => "img-responsive" %></p>
	
	<p><%= link_to 'Show', product %></p>
	<p><%= link_to 'Edit', edit_product_path(product) %></p>
	<p><%= link_to 'Destroy', product, method: :delete, data: { confirm: 'Are you sure?' } %></p>

</div>