<div class="store-wrapper">
	<div class="store-inner">

		<div class="orders-main">
			<h2>Bulk orders:</h2>
			<p>(Yeah, I wish I'll be having bulk orders. lol)</p>	 




<!-- ANGULAR exercise -->
	  <div ng-controller="OrdersCtrl">
	  	
			<table class="table table-hover">
				<thead>
					<td>Order ID</td>
					<td>Total</td>
					<td>Product</td>
					<td></td>
				</thead>

				<tr ng-repeat="order in orders | orderBy:'-id':reverse">
					<td>
						{{order.id}}
					</td>
					<td>
						<strong>{{order.total | currency}}</strong>
					</td>
					<td>
						{{order.product_id}}
						<small ng-show="order.id"><br>-{{order.user_id}}</small>
					</td>
					<td>

					</td>
				</tr>

			</table>

		  <form class="form-inline" ng-submit="addOrder()">
		  	<strong>Add order: </strong>

		  	<input type="number" value="{{orders[orders.length - 1].id + 1}}" ng-model="newOrder.id">
		  	<input type="number" step="0.01" class="form-control" placeholder="Total" ng-model="newOrder.total">
		  	<input type="number" class="form-control" ng-model="newOrder.product_id">

		  	<input type="submit" value="+" class="btn btn-success">
		  </form>

		  <td>
		  	<button ng-click="deleteOrder(order)" class="btn btn-danger btn-sm"><span class="glyphicon glyphicon-trash" aria-hidden="true"></span></button>
		  </td>

	  </div>



<!-- ANGULAR exercise -->




 </div><!-- end store inner --> 
</div><!-- end store wrapper -->